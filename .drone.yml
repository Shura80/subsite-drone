---
kind: pipeline
name: agri-eip

platform:
  os: linux
  arch: amd64

# SERVICES #
services:
  - name: web
    image: fpfis/php56-build
    environment:
      DOCUMENT_ROOT: /test/toolkit/build
      PORT: 8080
      # TEST CUSTOM SECRETS.
      db.dl.username:
        from_secret: agri_user
      db.dl.password:
        from_secret: agri_password
    commands:
      - echo ${DB.DL.USERNAME}
      - echo ${DB.DL.PASSWORD}



